{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "migrate:db": "bun drizzle-kit generate:mysql",
    "push:db": "bun drizzle-kit push:mysql"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.30.6",
    "hono": "^4.2.0",
    "mysql2": "^3.9.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.20.14"
  }
}
